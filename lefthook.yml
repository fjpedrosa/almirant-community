pre-push:
  parallel: true
  commands:
    backend-tests:
      run: bun run scripts/run-backend-pre-push-tests.ts
    frontend-tests:
      run: cd frontend && bun install --frozen-lockfile && bun test
