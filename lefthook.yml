pre-push:
  parallel: true
  commands:
    backend-tests:
      run: cd backend/api && DATABASE_URL=${DATABASE_URL:-postgres://ci:ci@localhost:5432/ci} bun test
    frontend-tests:
      run: cd frontend && bun install --frozen-lockfile && bun test
