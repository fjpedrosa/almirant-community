# Optional override used by:
# docker compose --env-file .env.dev-prod -f docker-compose.yml -f docker-compose.dev-proddb.yml up -d

PROD_DATABASE_URL=postgresql://user:password@host:5432/database
PROD_ALMIRANT_API_KEY=alm_sa_replace_me
